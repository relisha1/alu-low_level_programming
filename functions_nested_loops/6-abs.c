#include "main.h"

/**
 * _abs - for the absolute value of an integer
 * @n: the integer whose absolute vlaue is computed
 *
 * Return: esult
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
